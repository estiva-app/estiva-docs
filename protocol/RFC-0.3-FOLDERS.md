# RFC 0.3 — Folders, Files, Components, Conversations *(superseded)*

**Superseded by [RFC 0.4 — The Workspace](RFC-0.4-WORKSPACE.md) on 2026-08-28.**

0.4 is this document's next version, not a companion to it. Everything 0.3
contained is carried forward there — the folder model, the two production
measurements (§5.2, §5.3), the rejected alternatives (§4.7), and the
answered-questions ledger — and **section numbers 1–12 are unchanged**, so any
reference to §4.2, §5.2, §5.3 or §10.1 resolves in 0.4 exactly as it did here.

This stub remains so existing links keep working, following the same pattern as
the pointer stubs left behind when `FILES_ARCHITECTURE.md` moved.

What 0.4 adds: the projection layer (§13), the message/rich-text split (§14),
and the third-app checklist (§15). What it changes: §6 is unfrozen, §7 carries
the `kind:9`→`kind:1111` correction, and §10.1's deferral trigger has fired.

The full text of 0.3 as it stood is in this repository's git history, at
`dce66dd:protocol/RFC-0.3-FOLDERS.md`.
