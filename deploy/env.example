# Copy to /opt/estiva-docs/.env on the box. Never commit a filled-in copy.
#
# The container refuses to start if DOCS_PASSWORD is empty, so this file is
# what stands between the docs and the open internet.

DOCS_USER=estiva
DOCS_PASSWORD=
